# Streamlit dashboard dependencies (deployed app)
#
# IMPORTANT: In Streamlit Cloud "Advanced settings", set Python version = 3.13.
# scikit-learn 1.7.1 (the version the models were pickled with) has NO prebuilt
# wheel for Python 3.14, so 3.14 forces a source build that fails (needs gfortran).
# On Python 3.13 everything installs from wheels — no compilers needed.
streamlit>=1.31
pandas>=2.0,<3
numpy>=1.24,<3
plotly>=5.18
scikit-learn==1.7.1
joblib>=1.3
