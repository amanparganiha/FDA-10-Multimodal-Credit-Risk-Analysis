# Streamlit dashboard dependencies (deployed app)
# scikit-learn is pinned to the version the models were trained/pickled with (1.7.1)
# so the .pkl model files load cleanly on Streamlit Cloud.
streamlit>=1.31
pandas>=2.0
numpy>=1.24
plotly>=5.18
scikit-learn==1.7.1
joblib>=1.3
