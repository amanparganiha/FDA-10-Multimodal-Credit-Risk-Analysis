paths:
  raw: data/raw
  processed: data/processed
  artifacts: data/processed/model_artifacts
  models: data/processed/model_artifacts/trained_models

data:
  processed_filename: credit_ratings_multimodal_final.csv
  rating_col: rating
  inv_grade_col: investment_grade

training:
  test_size: 0.2
  random_state: 42
  n_jobs: 1
  cv_folds: 3
  rf_n_estimators: 100
  gb_n_estimators: 100
